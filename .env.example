# 本地调试用。复制成 .env 后填入真实值，.env 已被 gitignore。
#
#   cp .env.example .env && vi .env
#   set -a && source .env && set +a && go run .
#
# 这样 token 不会出现在命令行里，也就不会进 shell 历史记录。

BDUSS=
TG_BOT_TOKEN=
TG_CHAT_ID=
